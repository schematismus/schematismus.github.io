[project]
name = "website"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ryland>=0.16.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
    "watchdog>=6.0.0",
]
